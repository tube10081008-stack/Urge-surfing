# Google AI Studio에서 발급한 Gemini API 키 (서버에만 보관).
GEMINI_API_KEY=your-api-key-here

# Live Translate 모델 ID. 가용 여부에 따라 교체 가능.
GEMINI_LIVE_MODEL=gemini-3.5-live-translate-preview

# 릴레이 접근 토큰(오픈 프록시 악용 방지). 빈 값이면 검사 안 함(개발용).
RELAY_TOKEN=change-me

# 바인딩 주소/포트.
RELAY_HOST=0.0.0.0
RELAY_PORT=8080
